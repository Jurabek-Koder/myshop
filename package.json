{
  "name": "myshop-render-workspace",
  "private": true,
  "scripts": {
    "dev": "concurrently -n backend,frontend -c cyan,magenta \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "local:preview": "npm run build:local-preview --prefix frontend && cross-env NODE_ENV=production npm start --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "cross-env": "^7.0.3"
  }
}
