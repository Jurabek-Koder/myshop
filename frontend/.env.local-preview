# `npm run build:local-preview` — Render monolit kabi: `.env.production` o‘qilmaydi,
# shuning uchun so‘rovlar odatda nisbiy `/api` (bir xil origin). Tashqi URL majburiy bo‘lsa — shu faylga qo‘shing.
