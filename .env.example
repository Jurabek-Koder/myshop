# Vite muhiti
# - Lokal: .env.local (masalan VITE_API_PROXY_TARGET) — Cursor / brauzerda http://localhost:5173
#   Ildizdan: npm run dev → backend + Vite birga; o‘zgarishlar darhol (HMR), Render deploy shart emas
# - Production build: .env.production — namuna: .env.production.example
#
# Batafsil: ../DEPLOY.md
